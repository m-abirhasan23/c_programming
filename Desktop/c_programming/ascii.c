#include <stdio.h>

int main()
{

    char inputValue;
    printf("Enter a second floating number: ");
    scanf("%c", &inputValue);
    printf("The ASCII value of %c is %d", inputValue, inputValue);

    return 0;
}